# INSANYCK Environment Variables — Fase D
# Copy this file to .env.local and fill in your actual values
# Never commit .env.local to version control

# ====== URLs (alinhar com a porta em uso) ======
NEXT_PUBLIC_URL=http://localhost:3000
NEXTAUTH_URL=http://localhost:3000

# Segredo NextAuth (>=32 chars). Gere com: openssl rand -base64 48
NEXTAUTH_SECRET=__SET_ME__

# De/Remetente padrão
EMAIL_FROM="INSANYCK <no-reply@insanyck.com>"

# ====== Stripe (FASE C) ======
STRIPE_SECRET_KEY=__SET_ME__
STRIPE_WEBHOOK_SECRET=__SET_ME__   # stripe listen --forward-to ... fornece o whsec
STRIPE_PUBLISHABLE_KEY=pk_test_51xxxxxxxxxxxxxxx
STRIPE_API_VERSION="2025-07-30.basil"

# ====== Toggle de backend ======
BACKEND_DISABLED=0

# ====== Google OAuth (opcional) ======
GOOGLE_CLIENT_ID=__SET_ME__
GOOGLE_CLIENT_SECRET=__SET_ME__
# Authorized redirect URI:
#   http://localhost:3000/api/auth/callback/google

# ====== E-mail transacional (escolher UM) ======
# Resend (prioritário no código)
RESEND_API_KEY=__SET_ME__

# Ou Amazon SES (fallback)
SES_REGION=__SET_ME__
SES_ACCESS_KEY_ID=__SET_ME__
SES_SECRET_ACCESS_KEY=__SET_ME__

# ====== Database ======
DATABASE_URL="postgresql://user:password@localhost:5432/insanyck"

# ====== Opcionais ======
NODE_ENV="development"  # development | production | test
INSANYCK_TEST_HIDE_UPDATE_BANNER=

# MongoDB (se usar cart-sync)
# MONGODB_URI="mongodb://localhost:27017/insanyck"

# OpenAI (se usar IA)
# OPENAI_API_KEY="sk-xxxxxxxxxxxxxxx"

# ====== PRODUÇÃO ======
# 1. Atualizar URLs para domínio real
# 2. Stripe: chaves live (pk_live_*, sk_live_*)
# 3. Webhook: https://yourdomain.com/api/stripe/webhook  
# 4. Database: apontar para prod
# 5. Email: configurar Resend ou SES real